# אפיון קצר לפרויקט

## מטרת הפרויקט

אפליקציית תקשורת תומכת וחליפית (תת"ח / AAC) המיועדת לעבודה במצב תצוגה אופקי (`Landscape`). המשתמש בוחר פרופיל, נכנס ללוח התקשורת שלו, בוחר מילים או תמונות ומרכיב מהן משפט שאפשר להשמיע.

אפליקציית Expo מחזיקה את המצב הפעיל בזיכרון באמצעות React Context, אך טוענת את הפרופילים, הלוחות והכרטיסים משרת ה־API. מסד הנתונים הוא מקור האמת, ולאחר כל כתיבה מוצלחת האפליקציה טוענת מחדש את מצב השרת. לא מתוכננת התמדה מקומית במכשיר.

## טכנולוגיות נוכחיות

- JavaScript ו־JSX
- React Native
- Expo SDK 57, לאחר שדרוג הדרגתי מ־SDK 54 דרך SDK 55 ו־SDK 56.
- Expo Router
- NativeWind
- React Context כמצב משותף בזיכרון
- Expo Speech
- Expo Image Picker
- Fetch API ו־`multipart/form-data`
- C# ו־.NET 9
- ASP.NET Core Web API עם Controllers
- ADO.NET באמצעות `Microsoft.Data.SqlClient`
- SQL Server Express ו־SQL Server Management Studio ‏(SSMS)

## מצב המערכת הנוכחי

נכון ל־5 בספטמבר 2026:

> סטטוס אימות: הפרויקט שודרג בהדרגה ל־Expo SDK 57. לאחר בדיקה פיזית באייפון ותיקון הבאגים שדווחו, סהר אישר שהכול עובד ותקין ואישר מיזוג ל־main. האישור מתייחס לגרסת ההדגמה דרך Expo Go.

- האפליקציה מוגדרת לתצוגת `Landscape` בלבד.
- מסך בחירת הפרופילים מציג את הכותרת "תפריט משתמשים" ומציג דרך React Context פרופילים שנטענו מה־API; לחיצה על פרופיל פותחת את הלוח שלו.
- מלבני הפרופילים הוגדלו כדי לשפר את הקריאות ולאפשר מקום ברור לתמונת פרופיל לצד השם.
- מצב העורך נשמר ב־React Context ומשותף למסך בחירת הפרופילים ולמסך הלוח; הפעלתו דורשת הזנת PIN קבוע בן ארבע ספרות בחלונית שמוצגת מעל המסך.
- הזנת ה־PIN משתמשת במקלדת המספרים ה־Native של הטלפון; לחיצה על הרקע הכהה שמחוץ לחלונית סוגרת את המקלדת בלי לסגור את חלונית ה־PIN.
- חלונית ה־PIN הלבנה מוצגת ברוחב 460 ובגובה מינימלי 280 כדי להסתיר בצורה ברורה את תוכן המסך שנמצא מאחוריה.
- מצב העורך במסך בחירת הפרופילים מאפשר יצירת פרופיל חדש, פתיחת היסטוריית המשפטים ופתיחת הגדרות המשתמש של כל פרופיל.
- ברשומת פרופיל במצב עורך, כפתורי "היסטוריה" ו"הגדרות" מוצגים בצמוד למלבן הפרופיל; מחיקת הפרופיל מתבצעת מתוך מסך ההגדרות בלבד.
- מסך יצירת פרופיל בודק שהשם אינו ריק, מאפשר לבחור תמונת פרופיל אופציונלית מהגלריה, יוצר את הפרופיל דרך ה־API, טוען מחדש את מצב השרת ומחזיר למסך הבחירה; התוכן מיושר לחלק העליון כדי ששדה השם יישאר גלוי בזמן שהמקלדת פתוחה.
- תמונת הפרופיל, אם קיימת, מוצגת לצד שם הפרופיל במסך הבחירה; במסך הגדרות המשתמש ניתן לשנות את שם הפרופיל, להחליף את התמונה או להסיר אותה.
- מסך "אודות" הושלם מבחינה עיצובית, מותאם ל־Landscape ונגיש ממסך בחירת הפרופילים.
- מסך "אודות" כולל הסבר על מטרת האפליקציה והנחיות שימוש בפרופילים, בהרכבת משפט, במחיקה, בהשמעה ובמצב העורך.
- מסכי בחירת הפרופילים, יצירת הפרופיל, הגדרות המשתמש ו"אודות" משתמשים כעת בשפה חזותית פסטלית עם טקסט כהה.
- לכל פרופיל נוצר לוח ראשי ריק עם גריד קבוע של 3 שורות ו־8 עמודות.
- שמירת כרטיס ניווט בלוח הראשי יוצרת עבורו לוח־בן ייחודי וריק עם 24 תאים.
- בלוחות־הבן ניתן ליצור כרטיסי תוכן בלבד; אין ניווט מקונן.
- הכרטיסים מיוצגים כאובייקטים הכוללים מזהה, מזהה לוח, סוג כרטיס, טקסט מוצג, טקסט להקראה, נתיב אופציונלי לתמונה, יעד ניווט ומיקום בגריד.
- לחיצה רגילה על כרטיס תוכן מוסיפה אותו לסוף שורת ההרכבה.
- לחיצה רגילה על כרטיס ניווט פותחת את לוח היעד ואינה מוסיפה את הכרטיס למשפט.
- לחיצה רגילה על תא ריק אינה מפעילה פעולה.
- כאשר מצב העורך פעיל, הלוח מציג חיווי חזותי ולחיצה ארוכה על תא פותחת מסך עריכה נפרד.
- מסך עריכת התא מציג את שני שדות הטקסט בחלק העליון וזה לצד זה, מאפשר להוסיף ידנית סימני ניקוד לטקסט להקראה, וכן לבחור תמונה אופציונלית מהגלריה או לצלם תמונה במצלמה, להחליף אותה או להסיר אותה.
- בחירה מהגלריה וצילום במצלמה מאפשרים תמונות בלבד וכוללים חיתוך ריבועי לפני השמירה.
- בלוח הראשי ניתן לבחור בין כרטיס תוכן לכרטיס ניווט; בלוח־בן הכרטיס נשמר תמיד ככרטיס תוכן.
- הפיכת כרטיס ניווט לכרטיס תוכן מקפיאה את לוח־הבן; החזרת הכרטיס לסוג ניווט פותחת שוב את אותו לוח עם התוכן שנשמר בו.
- ניתן למחוק כרטיס לאחר אישור; המחיקה מאפסת את נתוני הכרטיס ומוחקת גם את לוח־הבן הייחודי שלו, אם קיים.
- התמונות נתמכות בכרטיסי תוכן ובכרטיסי ניווט ומוצגות מעל הטקסט בכרטיס הלוח.
- בשורת ההרכבה כרטיס עם תמונה מוצג באמצעות התמונה בלבד, וכרטיס ללא תמונה מוצג באמצעות הטקסט שלו.
- הטקסט להקראה נשאר נפרד מהתצוגה וממשיך לקבוע את המשפט שנשלח ל־Expo Speech.
- שורת ההרכבה נגללת אופקית כאשר המשפט ארוך מהשטח הזמין.
- לחיצה רגילה על "מחק" מוחקת את המילה האחרונה ולחיצה ארוכה מנקה את כל המשפט.
- לחיצה על "נגן" מקריאה את המשפט בעברית באמצעות Expo Speech; במצב רגיל המשפט נשמר בהיסטוריה של הפרופיל, ובמצב עורך הוא אינו נשמר.
- לכל פרופיל קיימת הגדרה אישית להפעלת חיזוי המילה הבאה, והיא כבויה כברירת מחדל.
- כאשר החיזוי פעיל, אזור ההרכבה מתחלק לשורה רגילה ולשורת חיזוי שמציגה את המשפט הקיים ואת המילה החזויה; כאשר אין התאמה השורה נשארת ריקה.
- החיזוי משתמש בהיסטוריה האישית של הפרופיל, מנסה תחילה Trigram לפי שתי המילים האחרונות ונופל ל־Bigram לפי המילה האחרונה.
- לחיצה על "השמע חיזוי" מקריאה את המשפט החזוי ושומרת אותו בהיסטוריה במצב רגיל; במצב עורך הוא אינו נשמר.
- מסך היסטוריית המשפטים מציג את המשפטים של הפרופיל מהחדש לישן, כולל הטקסט שהוצג, הטקסט שהוקרא כאשר הוא שונה, ותאריך ההשמעה.
- כפתור "תפריט" חוזר למסך בחירת הפרופילים וכפתור "ראשי" מחזיר ללוח הראשי של הפרופיל הנוכחי.
- מסד הנתונים `AACCommunicationDb` הוקם ב־SQL Server Express ומנוהל ידנית דרך SSMS.
- מסד הנתונים כולל את הטבלאות `Profiles`,‏ `Boards`,‏ `CommunicationCards` ו־`SpokenSentences`, עם מפתחות זרים, בדיקות ואינדקסים מתאימים.
- לכל לוח נשמרות 24 רשומות ב־`CommunicationCards`, כולל תאים ריקים, כדי לשמור התאמה ישירה לגריד של 3 שורות ו־8 עמודות.
- הוקם פרויקט שרת ב־`server/AacApi` באמצעות ASP.NET Core Web API,‏ .NET 9 ו־Controllers.
- השרת מתחבר ישירות ל־SQL Server באמצעות ADO.NET ו־`Microsoft.Data.SqlClient`; אין שימוש ב־Entity Framework,‏ `DbContext`,‏ EF Migrations או ORM אחר.
- שאילתות שמקבלות קלט משתמש משתמשות ב־SQL Parameters ולא בחיבור מחרוזות.
- השרת מאפשר קריאה, יצירה, עדכון ומחיקה של פרופילים, קריאת לוחות וכרטיסים, עדכון כרטיסים ומחיקת תוכן של תא.
- יצירת פרופיל יוצרת באותו transaction גם לוח ראשי ו־24 תאים ריקים.
- שמירת כרטיס ניווט יוצרת באותו transaction לוח־בן ו־24 תאים, ושומרת את אותו `TargetBoardId` בעת הקפאה ושחזור.
- מחיקת כרטיס מאפסת את רשומת התא, ואם קיים לוח־בן היא מוחקת גם אותו ואת 24 הכרטיסים שלו באותו transaction.
- אפליקציית Expo פונה לשרת דרך כתובת הרשת המקומית שמוגדרת ב־`.env.local` תחת `EXPO_PUBLIC_API_URL`.
- `npm start` מפעיל תחילה את `set-local-api.ps1`, שמזהה את כתובת ה־IPv4 הפעילה של המחשב ומעדכן אוטומטית את `.env.local` עם פורט ה־API ‏`5229`.
- `ProfilesContext` טוען את הפרופילים, את הלוחות ואת היסטוריית המשפטים של פרופילים שהחיזוי פעיל עבורם, ואינו משתמש בנתוני התחלה מקומיים כמקור אמת. ה־`useEffect` היחיד באפליקציה משמש לטעינת הנתונים הראשונית מה־API פעם אחת בעת עליית ה־Provider.
- סקריפט הקמת המסד אינו יוצר פרופילי הדגמה; פרופילים נוצרים רק דרך האפליקציה.
- יצירה, עדכון ומחיקה של פרופילים, עדכון כרטיסים ומחיקת כרטיסים מתבצעים דרך ה־API; לאחר כל כתיבה מוצלחת הפרופילים והלוחות נטענים מחדש מהשרת.
- לא קיימת ולא מתוכננת התמדה מקומית באמצעות AsyncStorage או מסד נתונים במכשיר.
- תמונה חדשה שנבחרת ב־Expo מועלת לשרת כ־`multipart/form-data`, נשמרת תחת `wwwroot/uploads`, ורק הנתיב היחסי שלה נשמר ב־`ImagePath` במסד.
- השרת מגיש את קובצי התמונות באמצעות Static Files, והאפליקציה מחברת את הנתיב היחסי לכתובת ה־API לצורך התצוגה.
- מנגנון העלאת התמונות משמש תמונות של כרטיסי תקשורת ותמונות פרופיל אופציונליות בזמן יצירה או עדכון של פרופיל.
- משפטים שהושמעו במצב רגיל נשמרים בטבלת `SpokenSentences` ונטענים במסך היסטוריה ייעודי לכל פרופיל; השמעות במצב עורך אינן נשמרות.
- סקריפט ברירת המחדל `reset-project` הוסר מ־`package.json` משום שאינו משמש את הפרויקט.

## השינויים האחרונים שאושרו בבדיקת אייפון

### ניקוד הטקסט להקראה

- במסך עריכת תא קיימת שורת סימני ניקוד מתחת לשדה "טקסט שיוקרא".
- סימן הניקוד מתווסף במיקום הסמן בתוך הטקסט ואינו משנה את הטקסט שמוצג בכרטיס.
- הטקסט המנוקד נשמר בשדה `SpokenText` הקיים, ללא עמודה חדשה במסד הנתונים.
- הוספת הניקוד נכללה באישור הבדיקה של סהר. הכותרת היא "הוספת ניקוד" והכפתורים מוצגים בפריסה עוטפת ללא גלילה אופקית.

### חיזוי המילה הבאה

- הפעלת החיזוי נשמרת לכל פרופיל בנפרד בעמודה `PredictionEnabled`, וברירת המחדל היא כבוי.
- מנגנון החיזוי עצמו מחושב ישירות מהמשפט הנוכחי ומהיסטוריית הפרופיל, ללא `useEffect` נוסף עבור החיזוי.
- המנגנון מחפש תחילה התאמת Trigram לפי שתי המילים האחרונות; אם אין התאמה, הוא מחפש Bigram לפי המילה האחרונה.
- כאשר אין היסטוריה מספקת או שאין התאמה, שורת החיזוי נשארת ריקה ולא מוצגת מילה אקראית.
- כאשר החיזוי כבוי מוצגת שורת ההרכבה הרגילה בלבד. כאשר הוא פעיל, אזור ההרכבה מתחלק לשתי שורות דקות.
- שורת החיזוי אינה כוללת כפתור מחיקה. הכפתור "השמע חיזוי" מושבת כאשר אין חיזוי.
- מנגנון החיזוי נכלל באישור הבדיקה של סהר. כפתור "השמע חיזוי" כולל `will-change-pressable` כדי למנוע את אזהרת ReactNativeCss בעת מעבר בין מצב מושבת לפעיל.

### התאמות למסכים בזמן פתיחת המקלדת

- במסך יצירת פרופיל שדה שם הפרופיל הועבר לחלק העליון ונשמר בגובה מינימלי קריא.
- במסך עריכת תא השדות "טקסט שמוצג בכרטיס" ו־"טקסט שיוקרא" הועברו לחלק העליון והוצבו זה לצד זה.
- גובה שדות הקלט אושר באייפון ונשמר גם בתיקון פריסת הניקוד.

### העלאת תמונות ב־SDK 57

- סקריפט ההפעלה מגדיר ב־`.env.local` גם `EXPO_PUBLIC_USE_RN_FETCH=1`, כדי לתמוך בהעלאת קובץ מקומי באמצעות `uri` בתוך `FormData`.
- תיקון העלאת התמונות לפרופילים ולכרטיסים נכלל באישור הבדיקה של סהר.
- מסד הנתונים והתמונות המקומיות נשמרו ללא איפוס.

### הקמת מסד הנתונים

- סקריפט ההקמה יוצר מסד נקי ללא פרופילי הדגמה וללא נתוני Seed.
- פרופילים לדוגמה, אם נדרשים, נוצרים ידנית דרך האפליקציה בלבד.
- עמודת `PredictionEnabled` היא השינוי היחיד שנדרש בסכמת הפרופילים עבור מנגנון החיזוי.

## מבנה מסד הנתונים

הסכמה מופיעה גם בתרשים `SQLtables.png`. התרשים מתאר את מבנה הטבלאות ברמה מושגית; הטיפוסים, ברירות המחדל והאילוצים הממומשים מפורטים להלן. הקשר של `TargetBoardId` אל `Boards.Id` והעמודה `PredictionEnabled` עדיין אינם מסומנים בתרשים.

### Profiles

מכילה את הפרופילים הקיימים במערכת.

- `Id` — מסוג `INT IDENTITY`, המפתח הראשי של הפרופיל.
- `Name` — מסוג `NVARCHAR(100)`, שדה חובה שאינו יכול להיות ריק או להכיל רווחים בלבד.
- `ImagePath` — מסוג `NVARCHAR(1000)`, נתיב אופציונלי לתמונת הפרופיל.
- `PredictionEnabled` — מסוג `BIT`, שדה חובה עם ברירת מחדל `0`, הקובע האם שורת החיזוי מוצגת עבור הפרופיל.
- `CreatedAt` — מסוג `DATETIME2`, שדה חובה עם ברירת מחדל של זמן UTC בשרת.

### Boards

מכילה את לוחות התקשורת השייכים לפרופילים. לוח יכול להיות לוח ראשי או לוח פנימי שנפתח מתוך לוח אחר.

- `Id` — מסוג `INT IDENTITY`, המפתח הראשי של הלוח.
- `ProfileId` — מסוג `INT`, מפתח זר חובה אל `Profiles.Id`.
- `ParentBoardId` — מסוג `INT`, מפתח זר אופציונלי אל `Boards.Id`; בלוח ראשי הערך הוא `NULL` ובלוח־בן הוא מצביע ללוח הראשי.
- `Name` — מסוג `NVARCHAR(100)`, שדה חובה.
- `IsRoot` — מסוג `BIT`, מציין האם זהו הלוח הראשי של הפרופיל.
- `CreatedAt` — מסוג `DATETIME2`, שדה חובה עם ברירת מחדל של זמן UTC בשרת.

### CommunicationCards

מכילה את כרטיסי התקשורת המוצגים בתאים של כל לוח.

- `Id` — מסוג `INT IDENTITY`, המפתח הראשי של הכרטיס.
- `BoardId` — מסוג `INT`, מפתח זר חובה אל `Boards.Id`.
- `CardType` — מסוג `NVARCHAR(20)`, מוגבל לערכים `content` ו־`navigation`.
- `TargetBoardId` — מסוג `INT`, מפתח זר אופציונלי אל `Boards.Id`; הקישור נשמר גם כאשר לוח־הבן מוקפא זמנית.
- `Label` — מסוג `NVARCHAR(100)`, שדה חובה עם מחרוזת ריקה עבור תא ריק.
- `SpokenText` — מסוג `NVARCHAR(200)`, שדה חובה עם מחרוזת ריקה עבור תא ריק.
- `ImagePath` — מסוג `NVARCHAR(1000)`, נתיב יחסי אופציונלי לתמונה שמוצגת בכרטיס, לדוגמה `/uploads/example.jpg`.
- `SlotIndex` — מסוג `TINYINT`, מוגבל לטווח 0–23.
- `CreatedAt` — מסוג `DATETIME2`, שדה חובה עם זמן UTC.
- `UpdatedAt` — מסוג `DATETIME2`, שדה חובה שמתעדכן בעת שינוי או איפוס של התא.

### SpokenSentences

מכילה משפטים שהורכבו והושמעו עבור פרופיל.

- `Id` — מסוג `INT IDENTITY`, המפתח הראשי של המשפט.
- `ProfileId` — מסוג `INT`, מפתח זר חובה אל `Profiles.Id`.
- `DisplayText` — מסוג `NVARCHAR(2000)`, שדה חובה שאינו יכול להיות ריק.
- `SpokenText` — מסוג `NVARCHAR(2000)`, שדה חובה שאינו יכול להיות ריק.
- `CreatedAt` — מסוג `DATETIME2`, שדה חובה עם ברירת מחדל של זמן UTC בשרת.

### קשרים בין הטבלאות

- לכל רשומה ב־`Profiles` יכולים להיות לוחות רבים ב־`Boards`.
- ללוח ראשי ב־`Boards` יכולים להיות לוחות־בן שנוצרים מכרטיסי הניווט שלו.
- לכל לוח ב־`Boards` יכולים להיות כרטיסים רבים ב־`CommunicationCards`.
- לכל פרופיל ב־`Profiles` יכולים להיות משפטים רבים ב־`SpokenSentences`.
- `TargetBoardId` הוא מפתח זר אל `Boards.Id` וקושר כרטיס ללוח־הבן הייחודי שלו; הקשר אינו מסומן בקו בתרשים הנוכחי.
- אינדקס ייחודי על `(BoardId, SlotIndex)` מונע יותר מכרטיס אחד באותו תא.
- אינדקס ייחודי מסונן על `TargetBoardId` מונע מכמה כרטיסים להצביע לאותו לוח־בן.
- אינדקס ייחודי מסונן על לוח ראשי מונע יותר מלוח ראשי אחד לכל פרופיל.
- המפתחות הזרים אינם משתמשים ב־cascade delete; השרת מבצע מחיקות מפורשות בסדר הנכון בתוך transaction.

## שרת ה־API

- פתרון השרת נמצא ב־`server/AacApi/AacApi.sln` והפרויקט נמצא ב־`server/AacApi/AacApi`.
- השרת משתמש ב־ASP.NET Core Web API עם Controllers וב־.NET 9.
- מחרוזת החיבור המקומית מצביעה אל `SAHAR_PC\\SQLEXPRESS` ואל מסד הנתונים `AACCommunicationDb` באמצעות Windows Authentication.
- מחרוזת החיבור נשמרת ב־`appsettings.json`, אינה כוללת שם משתמש או סיסמה ומשמשת רק את סביבת ההדגמה המקומית.
- פרופיל ההפעלה `http` מאזין ב־`http://0.0.0.0:5229`, כך שטלפון באותה רשת מקומית יכול להגיע לשרת דרך כתובת ה־IPv4 של המחשב.
- `GET /api/profiles` — מחזיר את כל הפרופילים.
- `POST /api/profiles` — יוצר פרופיל, לוח ראשי ו־24 תאים ומחזיר `201 Created`.
- `PUT /api/profiles/{profileId}` — מעדכן את שם הפרופיל, נתיב התמונה האופציונלי והפעלת החיזוי ומחזיר את הפרופיל המעודכן.
- `GET /api/profiles/{profileId}/boards` — מחזיר את לוחות הפרופיל; לכל לוח מוחזר `cards` כמערך של 3 שורות ו־8 כרטיסים.
- `DELETE /api/profiles/{profileId}` — מוחק את הכרטיסים, המשפטים, הלוחות והפרופיל בתוך transaction ומחזיר `204 No Content`.
- `GET /api/profiles/{profileId}/spoken-sentences` — מחזיר את היסטוריית המשפטים של הפרופיל מהחדש לישן.
- `POST /api/profiles/{profileId}/spoken-sentences` — שומר משפט שהושמע עבור הפרופיל ומחזיר `201 Created`.
- `PUT /api/boards/{boardId}/cards/{cardId}` — מעדכן כרטיס תוכן או ניווט, יוצר לוח־בן לפי הצורך ומשמר לוח מוקפא.
- `DELETE /api/boards/{boardId}/cards/{cardId}` — מאפס את התא ומוחק את לוח־הבן שלו, אם קיים, ומחזיר `204 No Content`.
- `POST /api/images` — מקבל תמונת JPG, JPEG, PNG או WebP כ־`multipart/form-data`, שומר אותה בשם ייחודי תחת `wwwroot/uploads` ומחזיר נתיב יחסי.
- `UseStaticFiles` מאפשר לאפליקציה להציג את התמונות שהועלו דרך הנתיב `/uploads/{fileName}`.
- פעולות שמקבלות ערכים מהלקוח משתמשות ב־`SqlParameter` עם טיפוס ואורך מפורשים.
- פעולות שיוצרות או מוחקות כמה רשומות משתמשות ב־transaction עם commit או rollback.
- ה־endpoints נבדקו ידנית מול SQL Server באמצעות קובץ `AacApi.http`, בקשות HTTP מקומיות ו־Expo Go בטלפון פיזי.

## השלב הבא

- הבדיקה באייפון הושלמה ואושרה על ידי סהר; השדרוג ותיקוני הבאגים מאושרים למיזוג ל־`main`.
- להציג באפליקציה מצב טעינה והודעת שגיאה ידידותית כאשר ה־API אינו זמין, במקום רשימה ריקה ושגיאת console בלבד.
- אין צורך להגדיר CORS עבור Expo Go native; CORS יידרש רק אם תתווסף הרצה דרך דפדפן.

## גבולות ה־MVP וההדגמה

- האפליקציה מיועדת להצגה חד־פעמית דרך Expo Go ברשת המקומית ואינה מיועדת להתקנה או להפצה למכשירים.
- מסד הנתונים מוקם ומנוהל ידנית במחשב ההדגמה דרך SSMS; אין צורך בתהליך התקנה מלא או אוטומטי.
- אין צורך בבדיקות אוטומטיות או ב־CI במסגרת הפרויקט הנוכחי; הבדיקות מתבצעות ידנית.
- אימות משתמשים מלא, הקשחת העלאת קבצים ותצורת Production או הפצה אינם חלק מה־MVP.
- אין צורך למחוק קובצי תמונה ישנים שנשארים תחת `wwwroot/uploads` לאחר החלפה, הסרה או מחיקת רשומות.

## מסך בחירת פרופיל

- מציג את הכותרת "תפריט משתמשים" מעל רשימת הפרופילים.
- כל פרופיל מוצג במלבן מוגדל שמכיל את שם הפרופיל ותמונה אופציונלית לצדו.
- לחיצה על פרופיל פותחת את לוח התקשורת של אותו פרופיל.
- קיים מצב עורך שניתן להפעיל לאחר הזנת PIN נכון בחלונית מוגדלת; כיבוי מצב העורך אינו דורש PIN.
- שדה ה־PIN פותח את מקלדת המספרים ה־Native של הטלפון, ולחיצה על הרקע הכהה מחוץ לחלונית סוגרת את המקלדת בלבד.
- כאשר מצב העורך פעיל אפשר ליצור פרופיל חדש ולפתוח את היסטוריית המשפטים או את הגדרות המשתמש של כל פרופיל.
- כפתורי "היסטוריה" ו"הגדרות" נמצאים בצמוד למלבן הפרופיל; כפתור מחיקת הפרופיל אינו מוצג במסך זה.
- תמונת פרופיל אופציונלית מוצגת לצד שם הפרופיל.
- קיים כפתור מעבר למסך "אודות".

## מסך יצירת פרופיל

- כולל שדה להזנת שם הפרופיל.
- התוכן מיושר לחלק העליון ושדה השם נשאר בגובה קריא גם כאשר המקלדת פתוחה בתצוגה אופקית.
- מאפשר לבחור תמונת פרופיל אופציונלית מהגלריה, לחתוך אותה לריבוע או להסיר את הבחירה לפני השמירה.
- לא ניתן לשמור שם ריק או שם שמכיל רווחים בלבד.
- לחיצה על "שמור פרופיל" מעלה תחילה את התמונה שנבחרה, אם קיימת, יוצרת דרך ה־API את הפרופיל, הלוח הראשי ו־24 התאים, טוענת מחדש את מצב השרת ומחזירה למסך בחירת הפרופילים.
- לחיצה על "חזרה" מחזירה למסך בחירת הפרופילים ללא שמירה.

## מסך הגדרות משתמש

- נגיש ממסך בחירת הפרופילים באמצעות כפתור "הגדרות" שמוצג ליד הפרופיל כאשר מצב העורך פעיל.
- מציג את השם ואת תמונת הפרופיל הנוכחיים.
- מאפשר לשנות את שם הפרופיל, לבחור תמונה חדשה מהגלריה בחיתוך ריבועי או להסיר את התמונה הקיימת.
- מאפשר להפעיל או לכבות את שורת חיזוי המילה הבאה עבור הפרופיל הנוכחי.
- לא ניתן לשמור שם ריק או שם שמכיל רווחים בלבד.
- לחיצה על "שמור שינויים" מעלה תחילה תמונה חדשה, אם נבחרה, מעדכנת את הפרופיל דרך ה־API, טוענת מחדש את מצב השרת ומחזירה למסך בחירת הפרופילים.
- לחיצה על "חזרה" מחזירה למסך בחירת הפרופילים ללא שמירה.
- כפתור "מחק פרופיל" דורש אישור, מוחק את הפרופיל ואת כל הנתונים השייכים לו ומחזיר למסך בחירת הפרופילים.

## מסך אודות

- מותאם לתצוגת `Landscape` ומציג את התוכן בשני כרטיסי מידע זה לצד זה.
- מסביר בקצרה מהי תקשורת תומכת וחליפית ומה מטרת האפליקציה.
- כולל הנחיות לבחירת פרופיל, הרכבת משפט, תיקון המשפט והשמעתו.
- כולל הסבר על מצב העורך.
- כפתור "חזרה" מחזיר למסך בחירת הפרופילים.
- המסך הוא מסך מידע סטטי ואינו כולל לוגיקה נוספת.

## מסך עריכת תא

- נפתח בלחיצה ארוכה על תא כאשר מצב העורך פעיל.
- כולל בחלק העליון שני שדות זה לצד זה: טקסט שמוצג בכרטיס וטקסט שמוקרא, בגובה מינימלי זהה לשדה שבמסך ההגדרות.
- מתחת לטקסט שמוקרא מופיעה הכותרת "הוספת ניקוד" וכפתורי ניקוד בפריסה עוטפת ללא גלילה אופקית; העורך ממקם את הסמן אחרי אות ולוחץ על הסימן הרצוי.
- כולל תצוגה מקדימה של תמונת הכרטיס וכפתורים לבחירה מהגלריה, צילום במצלמה והסרה; המרווח האופקי בכפתורי "גלריה" ו"מצלמה" מותאם לרוחב הזמין כדי שהטקסט יוצג במלואו.
- בחירת תמונה מהגלריה וצילום במצלמה מוגבלים לתמונות ופותחים חיתוך ריבועי.
- לא ניתן לשמור כאשר אחד משני השדות ריק או מכיל רווחים בלבד.
- בלוח הראשי ניתן לבחור בין כרטיס תוכן לכרטיס ניווט; בלוח־בן אפשר לשמור כרטיסי תוכן בלבד.
- שמירת כרטיס ניווט חדש יוצרת עבורו אוטומטית לוח־בן ריק; יעד הניווט אינו נבחר ידנית.
- המרת כרטיס ניווט לתוכן מקפיאה את לוח־הבן שלו, והמרתו בחזרה לניווט משחזרת את הגישה לאותו לוח.
- לחיצה על "שמור" מעלה תחילה תמונה חדשה, אם נבחרה, מעדכנת את הכרטיס דרך ה־API, טוענת מחדש את מצב השרת ומחזירה ללוח שממנו נפתח המסך.
- לחיצה על "ביטול" חוזרת ללוח ללא שמירה.
- כפתור "מחק" מוצג בכרטיס שאינו ריק ודורש אישור; הוא מאפס את נתוני הכרטיס ומוחק את לוח־הבן שלו, אם קיים.

## מסך הפרופיל ולוח התקשורת

### ניווט עליון

- בצד ימין נמצא כפתור "ראשי", שמחזיר את המשתמש ללוח הראשי של הפרופיל הנוכחי.
- בצד שמאל נמצא כפתור "תפריט", שמחזיר למסך בחירת הפרופילים.

### שורת ההרכבה

- נמצאת לאורך החלק העליון של המסך, מתחת לכפתורי "ראשי" ו"תפריט".
- מוצגת כמלבן שבו מצטברים הפריטים שנבחרו מהלוח.
- בצד ימין נמצא כפתור "נגן" בגובה שורת ההרכבה.
- בצד שמאל נמצא כפתור "מחק" בגובה שורת ההרכבה.
- לחיצה רגילה על "מחק" מוחקת את הפריט האחרון בשורת ההרכבה.
- לחיצה ארוכה על "מחק" מנקה את כל שורת ההרכבה.
- כרטיס שנבחר ומכיל תמונה מוצג בשורת ההרכבה כתמונה בלבד; כאשר אין תמונה מוצג ה־`label`.
- לחיצה על "נגן" משמיעה את המשפט שנמצא בשורת ההרכבה.
- כל לחיצה על "נגן" במצב רגיל שומרת במסד רשומה חדשה של המשפט עבור הפרופיל הנוכחי.
- במצב עורך המשפט מושמע לצורך בדיקה אך אינו נשמר בהיסטוריה.
- ההקראה מורכבת מערכי `spokenText` של הכרטיסים ואינה תלויה בשאלה אם מוצגת תמונה או תווית.
- כאשר החיזוי פעיל, מתווספת שורה שנייה ודקה ללא כפתור מחיקה, ובה המשפט הקיים והמילה הבאה שנבחרה על ידי Trigram או Bigram.
- כאשר אין התאמה בהיסטוריה, שורת החיזוי נשארת ריקה וכפתור "השמע חיזוי" מושבת.
- השמעת חיזוי במצב רגיל שומרת את המשפט החזוי בהיסטוריה; השמעה במצב עורך אינה נשמרת.

### הלוח

- נמצא מתחת לשורת ההרכבה ומשתמש בשאר שטח המסך עד לתחתית.
- הלוח הראשי נוצר כלוח ריק, ולוחות־בן נוצרים אוטומטית מכרטיסי ניווט בלוח הראשי.
- בנוי מגריד קבוע של 3 שורות ו־8 עמודות, ובסך הכול 24 תאים.
- תא יכול להיות ריק או להציג `label` ותמונה אופציונלית מעליו.
- לחיצה רגילה על תא ריק אינה מפעילה פעולה.
- תמונות מוצגות באותו אופן בכרטיסי תוכן ובכרטיסי ניווט.
- לחיצה רגילה על כרטיס תוכן מוסיפה אותו לסוף שורת ההרכבה.
- לחיצה רגילה על כרטיס ניווט בלוח הראשי פותחת את לוח־הבן הייחודי שלו.
- בלוחות־הבן הכרטיסים הם כרטיסי תוכן בלבד.
- במצב העורך התאים מסומנים בצבע שונה ומוצג חיווי שמצב העריכה פעיל.
- לחיצה קצרה ממשיכה להפעיל את הכרטיס גם במצב העורך.
- לחיצה ארוכה על תא במצב העורך פותחת את מסך עריכת התא.

## מסך היסטוריית משפטים

- נגיש ממסך בחירת הפרופילים באמצעות כפתור "היסטוריה" שמוצג לצד הפרופיל כאשר מצב העורך פעיל.
- מציג רק את המשפטים של הפרופיל שנבחר.
- המשפטים מוצגים מהחדש לישן וכוללים את הטקסט שהוצג ואת זמן ההשמעה.
- כאשר הטקסט שהוקרא שונה מהטקסט שהוצג, מוצג גם הטקסט שהוקרא.
- המסך אינו מאפשר למחוק רשומות היסטוריה.
- מחיקת פרופיל מוחקת גם את כל רשומות ההיסטוריה שלו.

## כללי העבודה על הפרויקט

- מטרת העבודה היא לימוד JavaScript, JSX, React, React Native, C# וטכנולוגיות נוספות שיידרשו בהמשך.
- המשתמש כותב את קוד הפרויקט בעצמו.
- Codex מסביר, מנחה ונותן דוגמאות קוד קצרות וברורות, אך אינו בונה את הפרויקט במקום המשתמש.
- אין לשנות קובצי קוד או לבדוק את עבודת המשתמש באופן אוטומטי, אלא אם המשתמש מבקש זאת במפורש.
- כאשר המשתמש שולח שגיאה או קוד ומבקש עזרה, יש להסביר מה גורם לבעיה ואיך המשתמש יכול לתקן אותה.
- יש להתקדם ביחידות עבודה קטנות אך שלמות, בלי לפצל כל שינוי לחלקים זעירים שלא לצורך.
- אין להוסיף מסכים, פיצ'רים, התנהגויות או דרישות שהמשתמש לא הציע ואישר.
- אין לנפח את הקוד.
- יש לבחור בגישה הפשוטה ביותר שמתאימה לשלב הלימוד, גם אם קיימת גישה מקצועית או מתקדמת יותר.
- לפני החלטה חדשה שמשנה את התנהגות המוצר, יש להסביר את האפשרויות ולהמתין לבחירת המשתמש.
